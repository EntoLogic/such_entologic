exports.prependJsonArray = function(req, res, next) {
	//TODO Check if the response is a json array and prepend with )]}',
  // next();
};

exports.removeNotifications = function(req, res, next) {
	// next();
};